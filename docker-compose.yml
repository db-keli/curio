services:
  postgres:
    image: postgres:17
    container_name: curio-postgres
    environment:
      POSTGRES_DB: curio
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - curio-data:/var/lib/postgresql/data

volumes:
  curio-data:
